<?xml version="1.0" encoding="utf-8"?>
<!--
    menu_workout_detail.xml — Options menu for the WorkoutDetailActivity.

    Contains the "Delete Workout" action that appears in the toolbar overflow menu.
    Using showAsAction="never" keeps it in the overflow to prevent accidental taps
    on this destructive action.
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_delete_workout"
        android:title="@string/delete_workout"
        app:showAsAction="never" />

</menu>
