<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml — Main activity layout with BottomNavigationView.

    Uses CoordinatorLayout as the root to enable FAB and Snackbar coordination.
    The MaterialToolbar at the top changes title based on the selected tab.
    FragmentContainerView hosts the three main fragments (workouts, progress, library).
    BottomNavigationView at the bottom provides tab-based navigation.

    Design Decision: We use a fixed BottomNavigationView (not scrollable tabs)
    because we have exactly 3 destinations — the ideal count for bottom nav
    according to Material Design guidelines (3-5 items recommended).
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinatorLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- AppBarLayout wraps the Toolbar for proper scroll behavior -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:titleTextColor="@android:color/white"
            app:layout_scrollFlags="scroll|enterAlways" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Fragment container — hosts WorkoutListFragment, ProgressFragment, or ExerciseLibraryFragment.
         The layout_behavior ensures this view scrolls below the AppBarLayout properly. -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- BottomNavigationView for switching between the three main screens.
         Uses layout_gravity="bottom" to anchor it at the screen bottom.
         The menu items are defined in res/menu/bottom_nav_menu.xml. -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom"
        android:background="?android:attr/windowBackground"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/colorPrimary"
        app:itemTextColor="@color/colorPrimary" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
