<?xml version="1.0" encoding="utf-8"?>
<!--
    item_exercise_library.xml — Card for an exercise in the library screen.

    Displays:
    - Exercise name (bold, primary text)
    - Times performed count (secondary text)

    Simple card layout for the exercise library list. Each card represents
    a unique exercise the user has logged at least once.

    TODO: Add muscle group icon/badge
    TODO: Add last performed date
    TODO: Add best lift (max weight ever used)
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_small"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_medium">

        <!-- Exercise name — primary identifier -->
        <TextView
            android:id="@+id/textExerciseName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface" />

        <!-- Times performed count — shows exercise frequency -->
        <TextView
            android:id="@+id/textTimesPerformed"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textSize="13sp"
            android:textColor="@android:color/darker_gray" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
